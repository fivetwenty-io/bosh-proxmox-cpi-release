package agent

// fake_raw_test.go intentionally left empty after Raw() was removed from the
// pve.Client interface. The fakeRawClient type that lived here was deleted as
// part of the configdrive Raw() → typed-service migration.
