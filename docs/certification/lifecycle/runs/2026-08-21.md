# Lifecycle Run Report: 2026-08-21

<!-- lifecycle-run-meta {"command_line": "./scripts/test integration lifecycle", "config": "ci/integration.yml", "cpi_version": "v0.3.0-12-g17d5492-dirty", "date": "2026-08-21", "exit_code": 0, "finished": "2026-08-21 10:48:47", "passes_failed": 0, "passes_run": 4, "pve_version": "9.2.4", "started": "2026-08-21 10:30:33", "stemcell": "bosh-stemcell-1.364-openstack-kvm-ubuntu-noble.tgz", "verdict": "PASSED", "wall_hms": "18m14s"} -->

`./scripts/test integration lifecycle` generated this report from a live run of the CPI lifecycle harness (`scripts/lifecycle`) against a Proxmox VE lab. The harness exercises every CPI method a BOSH Director calls during a deploy, redeploy, and teardown cycle, one pass per configured disk storage pool, network mode, and snapshot-detach mode. See [BOSH CPI Certification](../../index.md) for how we run it.

## Verdict

**Result: PASSED.** 4 lifecycle passes run, 0 failed.

- Wall clock: 18m14s (2026-08-21 10:30:33 to 2026-08-21 10:48:47)

- Command: `./scripts/test integration lifecycle`

- Exit code: 0

## Timings

Each step below is one full pass of the lifecycle harness under the named configuration: `base` uses the synthesized CPI config as-is, `pool:` passes override the disk storage pool, `network:` passes exercise create_network and delete_network under the named mode, and `snapshot-detach-bypass` detaches with a snapshot still present.

| Step | Result | Duration |
|---|---|---|
| base | PASS | 4m35s |
| network:bridge | PASS | 5m11s |
| network:sdn | PASS | 4m14s |
| snapshot-detach-bypass | PASS | 4m09s |

## Environment

| Item | Value |
|---|---|
| CPI release | v0.3.0-12-g17d5492-dirty |
| Proxmox VE | 9.2.4 |
| Stemcell | bosh-stemcell-1.364-openstack-kvm-ubuntu-noble.tgz |
| Config | ci/integration.yml |

## Artifacts

The harness streams its output to the console and keeps no machine-readable artifacts. This document is the committed record.

## Reduced-ACL lifecycle pass

A second full lifecycle pass on the same day validates the pool-scoped permission model in [pve-api-permissions.md §7](../../../pve-api-permissions.md): a dedicated `pve` realm user with a `privsep=0` token, granted only the reduced ACL table (pool-scoped `BoshOperator` on the VM and template pools, `BoshBridgeUse` on `/sdn/zones/localnetwork`, `PVEAuditor` on `/`, the storage grants, and `Pool.Allocate`/`Pool.Audit` at `/pool`) — with no `/vms` grant at all.

| Item | Value |
|---|---|
| Result | PASS — all 16 steps, exit 0 |
| Strategy overrides | `detached_disk_strategy: free` (parkers live outside pools; see §7 caveats) |
| Pools | `vm_pool` and `stemcell_template_pool` both created on demand by the reduced token |
| Cleanup | `delete_vm`, `delete_stemcell`, and the empty-pool reaper all succeeded pool-scoped |

The pass drove three CPI changes now in the tree: API-token normalization accepts the `PVEAPIToken=` prefixed form, template creation passes `pool` in the create call (pool-scoped `VM.Allocate`), and identity tags are applied after create instead of in it (PVE's create-time tag check has no pool fallback). It also surfaced the `SDN.Use`, `VM.Config.CDROM`, `VM.Config.CPU`, `VM.Config.Memory`, and `VM.Config.HWType` requirements now documented in the §2 privilege table.
