# BATS Run Report: 2026-09-05

<!-- bats-run-meta {"bats_ref": "252eb20", "command_line": "./scripts/bats run --env cpitest", "cpi_version": "v0.5.2-1-ged12da1-dirty", "date": "2026-09-05", "director_cpi_release": "bosh-proxmox-cpi-0.5.2.tgz", "director_version": "282.1.13", "doc_suffix": "", "dual_stack": "no; IPv4 only", "env": "cpitest", "examples": 47, "excluded_reasons": {"dual_stack": "the stemcell's agent writes one systemd-networkd file per static interface configuration, keyed by interface name, so the second static address on a shared NIC overwrites the first and the agent's own static-address validation then fails; upstream grouped only the DHCP configurations (RFC0038). Run --only ipv6 to re-check once the agent groups the static ones too", "ipv6": "the stemcell's agent writes one systemd-networkd file per static interface configuration, keyed by interface name, so the second static address on a shared NIC overwrites the first and the agent's own static-address validation then fails; upstream grouped only the DHCP configurations (RFC0038). Run --only ipv6 to re-check once the agent groups the static ones too", "ipv6_manual_networking": "the stemcell's agent writes one systemd-networkd file per static interface configuration, keyed by interface name, so the second static address on a shared NIC overwrites the first and the agent's own static-address validation then fails; upstream grouped only the DHCP configurations (RFC0038). Run --only ipv6 to re-check once the agent groups the static ones too", "ipv6_prefix_allocation": "IPv6 prefix delegation is modeled only by the upstream AWS template; PVE has no prefix-delegation API for the CPI to call", "multiple_manual_networks": "needs a second manual network in the job, which only the dual-stack pass deploys; that pass runs under --only ipv6, with the IPv6 keys set in ci/integration.yml", "nic_groups": "the upstream example never enables IPv6, so its job keeps one network and the same-group-same-interface assertion holds for a single interface no matter what the CPI does; grouping is covered by the CPI's own tests, not by this suite", "raw_ephemeral_storage": "PVE VMs have no raw instance-store devices", "raw_instance_storage": "PVE VMs have no raw instance-store devices", "root_partition": "needs an IaaS flavor with no ephemeral disk; PVE vm_types size the root disk explicitly", "vip_networking": "PVE has no floating IP or VIP allocation concept for the CPI to bind"}, "excluded_tags": ["dual_stack", "ipv6", "ipv6_manual_networking", "ipv6_prefix_allocation", "multiple_manual_networks", "nic_groups", "raw_ephemeral_storage", "raw_instance_storage", "root_partition", "vip_networking"], "exit_code": 0, "failures": 0, "finished": "2026-09-05 06:23:48", "pve_version": "9.2.4", "ruby_version": "ruby 3.3.12", "skipped": 11, "started": "2026-09-05 04:02:15", "stemcell": "bosh-proxmox-kvm-ubuntu-noble-go_agent-light/1.383", "stemcell_mode": "light (preuploaded)", "verdict": "PASSED", "wall_hms": "2h21m33s"} -->

`./scripts/bats` generated this report from a live run of the [BOSH Acceptance Tests](https://github.com/cloudfoundry/bosh-acceptance-tests) against a Proxmox VE lab. See [Running BATS](../../bats.md) for how we run the suite.

## Verdict

**Result: PASSED.** 47 examples, 0 failures, 0 errors, and 11 skipped.

- Wall clock: 2h21m33s (2026-09-05 04:02:15 to 2026-09-05 06:23:48)

- Command: `./scripts/bats run --env cpitest`

- Exit code: 0

## Timings

The wall clock above covers the whole run. The tables below break it down by phase and by harness step; per-example durations appear in the Examples table. The report and artifact writing steps run after this report is rendered, so they are not listed.

| Phase | Duration |
|---|---|
| preflight | 0.8s |
| prepare | 23.0s |
| bats (rspec) | 2h21m09s |
| finalize | 0.9s |

| Step | Result | Duration |
|---|---|---|
| tool:uv | PASS | 0.0s |
| tool:bosh | PASS | 0.0s |
| tool:git | PASS | 0.0s |
| tool:ssh-keygen | PASS | 0.0s |
| file:ci/integration.yml | PASS | 0.0s |
| file:manifests/bosh/vars.yml | PASS | 0.0s |
| file:manifests/bats/cloud_config_pve.yml.erb | PASS | 0.0s |
| checkout:bosh-acceptance-tests | PASS | 0.4s |
| director:reachable | PASS | 0.1s |
| director:idle | PASS | 0.1s |
| director:addons | PASS | 0.2s |
| patch:cloud-config-template | PASS | 0.0s |
| patch:gemfile-junit-formatter | PASS | 0.0s |
| patch:ssh-keys-only | PASS | 0.0s |
| bundle:install | PASS | 21.8s |
| stemcell:resolve | PASS | 1.2s |
| bat.yml:render | PASS | 0.0s |
| bats:rspec:ipv4 | PASS | 2h21m09s |
| director:restore-cloud-config | PASS | 0.3s |

## Environment

| Item | Value |
|---|---|
| CPI release | v0.5.2-1-ged12da1-dirty |
| Director CPI release | bosh-proxmox-cpi-0.5.2.tgz |
| BATS revision | 252eb20 |
| BOSH director | 282.1.13 |
| Stemcell | bosh-proxmox-kvm-ubuntu-noble-go_agent-light/1.383 |
| Stemcell source | light (preuploaded) |
| Proxmox VE | 9.2.4 |
| Ruby | ruby 3.3.12 |
| Env bundle | cpitest |
| Dual stack | no; IPv4 only |

## Tag exclusions

We excluded the following rspec tags. Every IaaS provider excludes the capability tags that do not map to its platform; the table below lists each exclusion with its reason so the covered surface stays explicit.

| Tag | Reason |
|---|---|
| `dual_stack` | the stemcell's agent writes one systemd-networkd file per static interface configuration, keyed by interface name, so the second static address on a shared NIC overwrites the first and the agent's own static-address validation then fails; upstream grouped only the DHCP configurations (RFC0038). Run --only ipv6 to re-check once the agent groups the static ones too |
| `ipv6` | the stemcell's agent writes one systemd-networkd file per static interface configuration, keyed by interface name, so the second static address on a shared NIC overwrites the first and the agent's own static-address validation then fails; upstream grouped only the DHCP configurations (RFC0038). Run --only ipv6 to re-check once the agent groups the static ones too |
| `ipv6_manual_networking` | the stemcell's agent writes one systemd-networkd file per static interface configuration, keyed by interface name, so the second static address on a shared NIC overwrites the first and the agent's own static-address validation then fails; upstream grouped only the DHCP configurations (RFC0038). Run --only ipv6 to re-check once the agent groups the static ones too |
| `ipv6_prefix_allocation` | IPv6 prefix delegation is modeled only by the upstream AWS template; PVE has no prefix-delegation API for the CPI to call |
| `multiple_manual_networks` | needs a second manual network in the job, which only the dual-stack pass deploys; that pass runs under --only ipv6, with the IPv6 keys set in ci/integration.yml |
| `nic_groups` | the upstream example never enables IPv6, so its job keeps one network and the same-group-same-interface assertion holds for a single interface no matter what the CPI does; grouping is covered by the CPI's own tests, not by this suite |
| `raw_ephemeral_storage` | PVE VMs have no raw instance-store devices |
| `raw_instance_storage` | PVE VMs have no raw instance-store devices |
| `root_partition` | needs an IaaS flavor with no ephemeral disk; PVE vm_types size the root disk explicitly |
| `vip_networking` | PVE has no floating IP or VIP allocation concept for the CPI to bind |

## Examples

| Example | Status | Duration |
|---|---|---|
| auditd, sshd, cron, rsyslogd should be running | PASSED | 6m51s |
| cck unresponsive agent recreate_vm should replace vm and keep the persistent disks | PASSED | 10m42s |
| cck unresponsive agent recreate_vm_without_wait should replace vm | PASSED | 10m19s |
| cck unresponsive agent reboot_vm should reboot vm | PASSED | 8m16s |
| cck unresponsive agent delete_vm should delete vm | PASSED | 6m01s |
| cck unresponsive agent delete_vm_reference should delete vm reference | PASSED | 10m15s |
| network configuration when using manual networking changes static IP address | PASSED | 4m39s |
| persistent disks with multiple disks attaches multiple disks | PASSED | 4m58s |
| persistent disks with an orphaned disk can attach the disk to a stopped instance | PASSED | 10m03s |
| service configuration runit when initially started after instance boot (before agent has been started) deletes /etc/service/monit | SKIPPED | 0.0s |
| service configuration runit when initially started after instance boot (before agent has been started) when monit dies restarts it | SKIPPED | 0.0s |
| service configuration runit when initially started after instance boot (before agent has been started) when the agent dies restarts it | SKIPPED | 0.0s |
| service configuration runit when restarted after agent has been started does not delete /etc/service/monit | SKIPPED | 0.0s |
| service configuration runit when restarted after agent has been started when monit dies restarts it | SKIPPED | 0.0s |
| service configuration runit when restarted after agent has been started when the agent dies restarts it | SKIPPED | 0.0s |
| service configuration systemd when initially started after instance boot (before agent has been started) when monit dies restarts it | PASSED | 2m07s |
| service configuration systemd when initially started after instance boot (before agent has been started) when the agent dies restarts it | PASSED | 1m56s |
| service configuration systemd when restarted after agent has been started when monit dies restarts it | PASSED | 2m07s |
| service configuration systemd when restarted after agent has been started when the agent dies restarts it | PASSED | 1m57s |
| service configuration agent when initially started after instance boot starts monit | PASSED | 13.7s |
| service configuration agent when initially started after instance boot mounts tmpfs to /var/vcap/data/sys/run | PASSED | 12.8s |
| service configuration agent when initially started after instance boot creates a symlink from /etc/sv/monit to /etc/service/monit | SKIPPED | 13.6s |
| service configuration agent when initially started after instance boot does not keep pre-existing pid files in sys/run after instance reboot | PASSED | 2m24s |
| service configuration agent when restarted by runit does not remount /var/vcap/data/sys/run | SKIPPED | 0.0s |
| service configuration agent when restarted by runit does not remove existing pid files | SKIPPED | 0.0s |
| service configuration agent when restarted by runit does not recreates a symlink from /etc/sv/monit to /etc/service/monit | SKIPPED | 0.0s |
| service configuration agent when restarted by runit does not restart monit | SKIPPED | 0.0s |
| service configuration agent when restarted by systemd does not remount /var/vcap/data/sys/run | PASSED | 2m05s |
| service configuration agent when restarted by systemd does not remove existing pid files | PASSED | 2m06s |
| service configuration agent when restarted by systemd does not restart monit | PASSED | 2m06s |
| service configuration monit when initially started by agent when a monitored process dies restarts it | PASSED | 10.7s |
| service configuration monit when restarted by runit/systemd when a monitored process dies restarts it | PASSED | 13.8s |
| service configuration monit when restarted by runit/systemd when monit is running can not be reached from the host | PASSED | 4.4s |
| service configuration monit when restarted by runit/systemd when monit is running when using the monit cli can reach the api and show a summary | PASSED | 2.2s |
| with release, stemcell and deployment agent should survive agent dying | PASSED | 1m35s |
| with release, stemcell and deployment ssh can bosh ssh into a vm | PASSED | 1.7s |
| with release, stemcell and deployment job should recreate a job | PASSED | 4m40s |
| with release, stemcell and deployment job should stop and start a job | PASSED | 20.0s |
| with release, stemcell and deployment logs should get agent log | PASSED | 1.7s |
| with release, stemcell and deployment logs should get job logs | PASSED | 1.7s |
| with release, stemcell and failed deployment A brand new deployment should stop the deployment if the canary fails | PASSED | 6m18s |
| with release, stemcell and failed deployment A deployment already exists should stop the deployment if the canary fails | PASSED | 6m48s |
| with release and stemcell and subsequent deployments with persistent disk size changing should migrate disk contents | PASSED | 2.3s |
| with release and stemcell and subsequent deployments general stemcell configuration should set vcap password | PASSED | 1.9s |
| with release and stemcell and subsequent deployments general stemcell configuration should not change the deployment on a noop | PASSED | 0.9s |
| with release and stemcell and subsequent deployments general stemcell configuration should use job colocation | PASSED | 2.1s |
| with release and stemcell and subsequent deployments general stemcell configuration should have network access to the vm using the manual static ip | PASSED | 1.7s |

## Artifacts

The runner writes this run's machine-readable artifacts (rspec junit.xml, harness results.json, and the full console log) to the gitignored `.e2e-results/` directory; it does not commit them. This document is the committed record.
